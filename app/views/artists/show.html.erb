<h1> <%= @artist.name %></h1>
<% songs = @artist.songs %>
<%= pluralize(@artist.song_count, 'song' )%>
<ul>
  <% songs.each do |song| %>
      <li> <%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
